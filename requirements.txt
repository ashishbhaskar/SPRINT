#cupy==12.3.0 # This library is suppose to be install as per the GPU's driver version more info can be found here (https://docs.cupy.dev/en/stable/install.html)
numpy==1.26.2
pandas==2.1.4
scipy==1.11.4
termcolor==2.4.0
